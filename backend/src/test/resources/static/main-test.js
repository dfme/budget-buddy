// Test-Fixture für SpaRoutingTest (INFRA-05): steht stellvertretend für die gehashten
// Angular-Bundles (z.B. main-*.js), die im Prod-Build flach unter static/ liegen. Belegt,
// dass statische JS-Assets ohne Auth per GET (/*.js) ausgeliefert werden. Nicht im Prod-JAR.
export const spaAssetFixture = true;
