<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.3</version>
        <relativePath/>
    </parent>

    <groupId>com.budgetbuddy</groupId>
    <artifactId>budgetbuddy</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>BudgetBuddy</name>
    <description>Safe-to-Spend budgeting web app for students in Switzerland</description>

    <properties>
        <java.version>25</java.version>
        <jjwt.version>0.12.6</jjwt.version>
        <springdoc.version>2.8.17</springdoc.version>
        <anthropic.version>2.31.0</anthropic.version>
        <pdfbox.version>3.0.5</pdfbox.version>
    </properties>

    <dependencies>
        <!-- Web / REST -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Persistence -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <!-- PostgreSQL-Treiber (DB-05, ADR-12). Kein hibernate-community-dialects mehr:
             Hibernate bringt den PostgreSQLDialect selbst mit und erkennt ihn über die
             JDBC-Metadaten — deshalb steht auch keine dialect-Property in den Properties. -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Migrations (DB-01): Flyway-Version von Spring Boot gemanaged (11.x). Seit Flyway 10
             liegt der Datenbank-Support in eigenen Modulen — flyway-core allein kennt Postgres
             nicht und bricht beim Start mit "Unsupported Database" ab. -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Monitoring -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- API-Dokumentation (Swagger UI) -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- KI: Anthropic Claude -->
        <dependency>
            <groupId>com.anthropic</groupId>
            <artifactId>anthropic-java</artifactId>
            <version>${anthropic.version}</version>
        </dependency>

        <!-- PDF-Parsing -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>${pdfbox.version}</version>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers (DB-05): Integrationstests laufen gegen dieselbe Postgres-Version
             wie Produktion. Voraussetzung ist ein laufender Docker-Daemon — lokal wie in CI
             (GitHub-hosted Runner bringen ihn mit). Version von Spring Boot gemanaged.

             Bewusst OHNE org.testcontainers:junit-jupiter: dessen @Testcontainers/@Container
             starten einen Container pro Testklasse. PostgresTestDatabase hält stattdessen einen
             einzigen Container für die ganze JVM und isoliert über eine Datenbank je Klasse.
             Fehlt die Abhängigkeit, greift niemand versehentlich zur teureren Variante. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- Dummy-JWT_SECRET für alle Tests: JwtProperties erzwingt ein nicht-leeres
                 Secret (Fail-fast). Hier zentral gesetzt, damit jeder Test-Kontext
                 (test/prod/default-Profil) startet, ohne von einer lokalen .env abzuhängen.
                 KEIN echtes Secret — nur für Tests. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <environmentVariables>
                        <JWT_SECRET>test-only-secret-not-for-production-0123456789</JWT_SECRET>
                    </environmentVariables>
                    <systemPropertyVariables>
                        <!--
                            Docker-API-Version für Testcontainers (DB-05). Das in Testcontainers
                            1.21.x gebündelte docker-java handelt ohne diese Angabe API 1.32 aus.
                            Docker Engine 29 meldet MinAPIVersion 1.40 und beantwortet 1.32 mit
                            HTTP 400 — Testcontainers meldet das als "Could not find a valid Docker
                            environment" und verdeckt damit die eigentliche Ursache.

                            1.44 entspricht Docker Engine 25 (Januar 2024) und liegt damit unter
                            allem, was Docker Desktop oder GitHub-Runner heute ausliefern. Beim
                            Anheben von Testcontainers prüfen, ob die Zeile entbehrlich wird.
                        -->
                        <api.version>1.44</api.version>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Prod-Build (INFRA-04): baut die Angular-SPA und bündelt den Build-Output
            als statische Ressourcen ins JAR (BOOT-INF/static), gemäss ADR-10
            (Single-Artifact, kein separater CDN-Deploy). Wird via `-Pprod` aktiviert;
            der Default-Build bleibt backend-only und damit schnell für die lokale
            Entwicklung.
        -->
        <profile>
            <id>prod</id>
            <properties>
                <frontend-maven-plugin.version>1.15.1</frontend-maven-plugin.version>
                <node.version>v22.12.0</node.version>
                <frontend.dir>${project.basedir}/../frontend</frontend.dir>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.github.eirslett</groupId>
                        <artifactId>frontend-maven-plugin</artifactId>
                        <version>${frontend-maven-plugin.version}</version>
                        <configuration>
                            <workingDirectory>${frontend.dir}</workingDirectory>
                            <installDirectory>${project.build.directory}</installDirectory>
                        </configuration>
                        <executions>
                            <execution>
                                <id>install-node-and-npm</id>
                                <goals>
                                    <goal>install-node-and-npm</goal>
                                </goals>
                                <phase>generate-resources</phase>
                                <configuration>
                                    <nodeVersion>${node.version}</nodeVersion>
                                </configuration>
                            </execution>
                            <execution>
                                <id>npm-ci</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <phase>generate-resources</phase>
                                <configuration>
                                    <arguments>ci</arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>npm-build</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <phase>generate-resources</phase>
                                <configuration>
                                    <arguments>run build -- --configuration production</arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!--
                        Kopiert den Angular-Build-Output in die statischen Ressourcen
                        des JAR. Läuft nach dem Frontend-Build (generate-resources) und
                        vor dem Spring-Boot-Repackage (package).
                    -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-frontend-build</id>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <phase>prepare-package</phase>
                                <configuration>
                                    <outputDirectory>${project.build.outputDirectory}/static</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${frontend.dir}/dist/budgetbuddy/browser</directory>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
