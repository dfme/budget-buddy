# [DB-05] Migration von SQLite auf Neon Postgres (inkl. Entscheid-ADR)

- **Issue:** [#89](https://github.com/dfme/budget-buddy/issues/89)
- **Task-ID:** `DB-05`
- **Branch:** `feature/DB-05-neon-postgres-migration`
- **Story:** — (kein `us-*`-Label)
- **Sprint:** Sprint 4
- **Bestätigt am:** 2026-08-09

## Entscheide vor der Umsetzung

| Punkt | Entscheid | Begründung |
| ----- | --------- | ---------- |
| ADR-Nummer | **ADR-12**, nicht ADR-11 wie im Issue | ADR-11 war beim Schreiben des Issues frei, ist inzwischen aber das UI-Design-System (`ADR-11-ui-design-system.md`). Eine Umnummerierung bräche externe Links und alle Verweise in `CLAUDE.md`. |
| Scope-Delta E2E + Migrationstests | **Im selben PR mitfixen**, im PR-Body als Erweiterung deklariert | Beide brechen zwingend durch die Umstellung (E2E setzt `SQLITE_DB_PATH`, die Migrationstests prüfen via `PRAGMA`). Ohne sie ist der AC „alle Tests grün, lokal **und** in CI" nicht erfüllbar. |
| Ersatz für `COLLATE NOCASE` | Pattern in `CategoryLearningService` upper-case normalisieren; Spalte bleibt plain `VARCHAR PRIMARY KEY` | Keine Postgres-Extension nötig, dialektunabhängig. `findMatching` ist über `upper()` ohnehin schon collation-unabhängig. Alternative `citext` verworfen: bindet die Migration an eine Extension. |
| Neon/Render | Ich liefere Code + Runbook, das Projekt wird parallel vom Team angelegt | Kein Zugang zu Neon/Render-Dashboard. Die zwei Infra-ACs bleiben im PR ausdrücklich offen. |

## Technische Eckwerte (verifiziert vor der Planung)

- Flyway ist real **11.7.2** (`./mvnw help:evaluate -Dexpression=flyway.version`), nicht 10.x wie
  in `CLAUDE.md` behauptet. Seit Flyway 10 sind die DB-Support-Module getrennt → zusätzlich
  `org.flywaydb:flyway-database-postgresql` nötig.
- `org.postgresql:postgresql` **42.7.7** und `org.testcontainers` **1.21.2** sind bereits
  Spring-Boot-managed → keine eigenen `<version>`-Angaben im `pom.xml`.
- Postgres-Version durchgängig **18**: `postgres:18-alpine` lokal, CI-Service-Container,
  Testcontainers. 18 ist laut
  [Neon Version Support Policy](https://neon.com/docs/postgresql/postgres-version-policy) der
  Default für neue Projekte (unterstützt: 14–18); eine ältere Version wäre nur über
  `--pg-version` erzwingbar. Entscheidend ist, dass lokal, CI und Neon dieselbe Major-Version
  fahren — sonst ist der Dialect-Mismatch nur verschoben statt beseitigt.

## Betroffene Files

### Neu

- `docs/adr/ADR-12-datenpersistenz-produktion.md`
- `docker-compose.yml` (Repo-Root)
- `backend/src/test/java/com/budgetbuddy/support/PostgresTestDatabase.java`

### Backend — Konfiguration

- `backend/pom.xml` — `sqlite-jdbc` und `hibernate-community-dialects` entfernen;
  `postgresql`, `flyway-database-postgresql`, `testcontainers:postgresql`,
  `testcontainers:junit-jupiter`, `spring-boot-testcontainers` ergänzen
- `application.properties` — Postgres-Default (`localhost:5432/budgetbuddy`), Dialect-Zeile entfällt
  (Hibernate erkennt Postgres selbst)
- `application-prod.properties` — SQLite-Kommentarblock ersetzen; dokumentiert
  `SPRING_DATASOURCE_URL` / `_USERNAME` / `_PASSWORD` aus der Render-Umgebung
- `application-test.properties` — Datasource-Zeilen entfernen (kommen dynamisch aus dem Container)

### Backend — Migrationen (in-place, keine V05+)

Es gibt keine zu erhaltenden Daten — die SQLite-Datei wird auf dem Free-Plan bei jedem Spin-Down
gelöscht. Neue Checksummen sind daher folgenlos.

- `V01` — `INTEGER PRIMARY KEY AUTOINCREMENT` → `BIGINT GENERATED BY DEFAULT AS IDENTITY`,
  `DEFAULT 0` → `DEFAULT FALSE`
- `V02`, `V03` — analog; `VARCHAR` → `TEXT` für Freitext, `DECIMAL(10,2)` bleibt (ADR-9)
- `V04` — `COLLATE NOCASE` entfällt, Seed-Daten unverändert

### Backend — Code

- `CategoryLearningService.java` — `trim().toUpperCase(Locale.ROOT)` vor dem Save; Javadoc statt
  NOCASE-Verweis
- `CategoryLookup.java` — Javadoc-Verweis auf `COLLATE NOCASE` korrigieren

### Backend — Tests

- 16 Klassen mit `@DynamicPropertySource`: Temp-File-Muster (`createTempDbFile()`, `jdbc:sqlite:…`)
  → `PostgresTestDatabase.register(registry, "<name>")`
- 5 Klassen mit `@ActiveProfiles("test")`: ebenfalls Container-Properties
- 4 Migrationstests: `PRAGMA table_info` / `pragma_index_list` → `information_schema.columns` /
  `pg_indexes`
- `ProdProfileSmokeTest` — `SQLITE_DB_PATH=:memory:` → Container-Properties

### E2E

- `e2e/support/backend.ts` — `DB_PATH` raus, JDBC-Koordinaten rein; `resetDatabase()` wird
  `DROP SCHEMA public CASCADE; CREATE SCHEMA public;`
- `e2e/playwright.config.ts` — `SQLITE_DB_PATH` → `SPRING_DATASOURCE_*`; Begründung für den
  Single-Worker neu formulieren (nicht mehr „SQLite hat einen Writer", sondern geteilte Test-DB)
- `e2e/package.json` — `pg` als devDependency; `e2e/.gitignore` und `e2e/README.md` nachziehen

### CI/CD/Doku

- `.github/workflows/build.yml` — `services: postgres` für den `e2e`-Job (das JAR braucht eine
  echte DB). Der `backend`-Job braucht nur Docker, das GitHub-hosted Runner mitbringen.
- `render.yaml` — `SPRING_DATASOURCE_URL/USERNAME/PASSWORD` mit `sync: false`, Kommentare korrigiert
- `README.md` — `docker compose up -d` als Schritt vor `./mvnw spring-boot:run`, Env-Tabelle
  (`SQLITE_DB_PATH` raus), Deployment-Abschnitt, Neon-Runbook
- `CLAUDE.md` — 13 Fundstellen: Tech-Stack-Tabelle, Abschnitt „SQLite + Spring Boot Gotchas",
  *What NOT to Use*, Testing-Tabelle, C2-Diagramm, Container-Tabelle, ADR-Liste
- `docs/adr/ADR-5-sqlite-mvp-database.md` → `Status: Superseded by ADR-12`
- `docs/adr/ADR-10-hosting-plattform.md` — Verweise auf die offene Persistenzfrage auf ADR-12 umbiegen
- `docs/adr/README.md` — Index-Zeile ADR-12, ADR-5-Status, vier Fliesstext-Stellen

## Implementierungsschritte

1. `pom.xml`: Dependencies tauschen
2. Flyway `V01`–`V04` auf Postgres-Syntax umschreiben
3. `application.properties`, `-prod`, `-test` umstellen
4. `CategoryLearningService`: Pattern-Normalisierung als NOCASE-Ersatz
5. `docker-compose.yml` mit Postgres 18 + benanntem Volume
6. `PostgresTestDatabase` (Singleton-Container, DB pro Testklasse) + alle 21 Testklassen umstellen
7. Migrationstests auf `information_schema` umschreiben
8. E2E: `backend.ts`, `playwright.config.ts`, `pg`-Dependency, README
9. `build.yml`: Postgres-Service-Container für den E2E-Job
10. ADR-12 schreiben; ADR-5, ADR-10, `docs/adr/README.md` nachziehen
11. `README.md`, `CLAUDE.md`, `render.yaml` nachziehen
12. Volle Suite lokal: `./mvnw verify` + E2E

## Test-Strategie

**`PostgresTestDatabase`** — ein einziger statischer `PostgreSQLContainer` pro JVM
(Singleton-Pattern statt `@Testcontainers` pro Klasse: sonst 21 Container-Starts statt einem).
`register(registry, name)` legt per `CREATE DATABASE` eine **eigene Datenbank** je Testklasse auf
diesem Container an. Das bildet die heutige Isolation (eine Temp-Datei pro Klasse) 1:1 ab, und
`@DirtiesContext` behält seine Bedeutung.

| Stufe | Inhalt |
| ----- | ------ |
| Migration | 4 bestehende Tests gegen `information_schema` — inkl. `numeric_precision = 10` / `numeric_scale = 2` für alle Geldspalten (ADR-9) und `is_identity` für die PKs |
| Integration | 12 bestehende `@SpringBootTest`-Klassen unverändert in der Aussage, nur Datasource getauscht |
| Neu | `CategoryLearningServiceIntegrationTest`: `learn("migros", …)` überschreibt den Seed `MIGROS`, statt eine zweite Zeile zu erzeugen — deckt den NOCASE-Ersatz ab |
| E2E | Playwright unverändert in der Aussage; `resetDatabase()` neu gegen Postgres |
| CI | `backend`-Job braucht nur Docker; `e2e`-Job bekommt einen `services: postgres`-Container |

Zielzustand: alle bestehenden Tests grün, plus ein neuer Test.

## Runbook — Neon + Render (manuell, parallel zur Umsetzung)

Identischer Text landet in `README.md` und ADR-12, damit er auffindbar bleibt.

```
1. neon.tech → Sign up → New Project
   · Name:    budget-buddy
   · Region:  Europe (Frankfurt)   ← nicht US, trägt die nDSG-Argumentation aus ADR-10
   · Postgres 18, Free Plan
2. Datenbank "budgetbuddy" anlegen (oder die Default-DB "neondb" verwenden)
3. Dashboard → Connect. Neon zeigt eine URI der Form
   postgresql://<user>:<password>@<host>.eu-central-1.aws.neon.tech/<db>?sslmode=require
   Ein Java-/JDBC-Snippet bietet Neon nicht an; die Umformung ist mechanisch (Schritt 5).
   `npx neonctl@latest init` ist dafür NICHT nötig — es scaffoldet JS-Projekte und legt
   .env-Dateien an, die in einem Spring-Boot-Repo nichts zu suchen haben.
4. Neon → Project settings → Members: mind. eine zweite Person einladen
   (der Render-Workspace bleibt auf Hobby = 1 Admin; dieser Engpass soll sich nicht verdoppeln)
5. Render → Service budgetbuddy → Environment → die URI aus Schritt 3 auf drei Variablen
   aufteilen:
   SPRING_DATASOURCE_URL       = jdbc:postgresql://<host>/<db>?sslmode=require
   SPRING_DATASOURCE_USERNAME  = <user>
   SPRING_DATASOURCE_PASSWORD  = <password>
   · "jdbc:" muss davor — die nackte postgresql://-URI nimmt der JDBC-Treiber nicht an.
   · User und Passwort gehören AUS der URL heraus in eigene Variablen: eingebettet landen
     sie in jedem Log, das die Datasource-URL ausgibt.
   · ?sslmode=require bleibt dran — Neon nimmt nur TLS-Verbindungen an.
   → NICHT ins Repo; render.yaml führt sie nur mit sync:false
6. Nach dem Merge: Deploy abwarten, User registrieren, "Manual Deploy → Clear build cache"
   auslösen, danach einloggen. Zweiter Test: 6 Min warten (Neon Scale-to-Zero) und erneut
   aufrufen — die Daten müssen beide Male da sein.
```

## Acceptance Criteria (aus dem Issue)

- [ ] Neon-Projekt existiert in Region Frankfurt/EU, Connection-String liegt als Umgebungsvariable
      in Render — kein Credential im Repo *(manuell, ausserhalb des PR)*
- [ ] ADR-12 existiert (inkl. der entfallenden SQLite-Vorteile unter `Consequences`), ADR-5 ist als
      `Superseded by ADR-12` markiert, `docs/adr/README.md` aktualisiert
- [ ] Flyway-Migrationen laufen gegen Postgres durch
- [ ] `hibernate-community-dialects` ist aus `pom.xml` entfernt
- [ ] `docker-compose.yml` existiert; `docker compose up -d` + `./mvnw spring-boot:run` startet die
      App lokal gegen Postgres
- [ ] `README.md` beschreibt das neue lokale Setup vollständig
- [ ] Integrationstests laufen gegen Testcontainers PostgreSQL, alle Tests grün — lokal und in CI
- [ ] `CLAUDE.md`, `render.yaml` und `README.md` beschreiben den Postgres-Stand
- [ ] Deployment auf Render verifiziert: Daten überleben Redeploy und Spin-Down
      *(manuell, ausserhalb des PR)*
